---
name: deployment-patterns
description: Planeja, implementa ou revisa build, pipelines, deploy, healthchecks, rollback e prontidão operacional conforme ADRs, custo zero, pipeline duplo e deploy automático de master.
---

# Adaptador da skill deployment-patterns

Todo output é em pt-BR.

Antes de agir, leia integralmente `docs/skills/deployment-patterns.md` e `docs/rules.md`. A definição canônica está no primeiro arquivo; este adaptador não acrescenta regras.
