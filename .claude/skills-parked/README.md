# Skills estacionadas

Fora do catálogo — **não custam tokens em nenhum prompt** (seção *Catálogos e arquivos permanentes* de `docs/rules.md`).
Reativar = mover para `.claude/skills/` na task que precisar. Mudança trivial, sem cerimônia.

| Skill | Reativar em |
|---|---|
| `design-system` | futura aplicação consumidora que definir design system |
| `frontend-patterns` | primeiro épico de UI |
| `benchmark` | futura aplicação consumidora que definir teste de carga e volumetria |
| `browser-qa` | coberto pela ferramenta de E2E que a aplicação escolher; reativar só se surgir QA exploratório manual |

Nenhuma fase de produto está aberta no `docs/roadmap.md`; o template não define fases.
